# 个人作品集网站

基于 React、Supabase 和 Netlify 构建的现代化个人作品集网站。

## 技术栈

- **前端**: React 18 + Tailwind CSS + React Router
- **后端**: Supabase (数据库 + 认证 + 存储)
- **部署**: Netlify (静态网站托管)

## 功能特性

### 📱 三个独立页面
1. **首页** - 个人介绍和技能展示
2. **项目展示** - 作品集展示和分类筛选
3. **联系页面** - 联系表单和社交链接

### 🗄️ 三张数据表
1. **profiles** - 个人信息表
   - 基本信息（姓名、职位、简介）
   - 技能列表
   - 头像图片

2. **projects** - 项目信息表
   - 项目详情（标题、描述、图片）
   - 技术栈标签
   - 分类筛选
   - 演示和源码链接

3. **contacts** - 联系信息表
   - 访客联系表单
   - 邮箱验证
   - 时间戳记录

## 快速开始

### 1. 克隆项目
```bash
git clone <repository-url>
cd portfolio-website
```

### 2. 安装依赖
```bash
npm install
```

### 3. 配置 Supabase
1. 在 [Supabase](https://supabase.com) 创建新项目
2. 复制项目 URL 和 anon key
3. 创建 `.env` 文件：
```bash
cp .env.example .env
```
4. 编辑 `.env` 文件，填入 Supabase 配置：
```
REACT_APP_SUPABASE_URL=https://your-project-id.supabase.co
REACT_APP_SUPABASE_ANON_KEY=your-anon-key-here
```

### 4. 运行数据库迁移
在 Supabase 控制台的 SQL 编辑器中，依次运行：
- `supabase/migrations/001_create_profiles.sql`
- `supabase/migrations/002_create_projects.sql`
- `supabase/migrations/003_create_contacts.sql`

### 5. 启动开发服务器
```bash
npm start
```
访问 http://localhost:3000

## 部署到 Netlify

### 自动部署（推荐）
1. 将代码推送到 GitHub 仓库
2. 在 Netlify 中连接 GitHub 仓库
3. 配置环境变量：
   - `REACT_APP_SUPABASE_URL`
   - `REACT_APP_SUPABASE_ANON_KEY`
4. 自动部署完成

### 手动部署
1. 构建项目：
```bash
npm run build
```
2. 上传 `build` 文件夹到 Netlify
3. 配置环境变量

## 项目结构

```
portfolio-website/
├── public/                 # 静态资源
├── src/
│   ├── components/         # 共享组件
│   │   └── Header.js
│   ├── config/            # 配置文件
│   │   └── supabase.js
│   ├── pages/             # 页面组件
│   │   ├── Home.js
│   │   ├── Projects.js
│   │   └── Contact.js
│   ├── services/          # 数据服务
│   │   └── database.js
│   ├── App.js
│   ├── App.css
│   ├── index.css
│   └── index.js
├── supabase/
│   └── migrations/        # 数据库迁移文件
├── .env.example
├── netlify.toml
├── package.json
├── tailwind.config.js
├── postcss.config.js
└── README.md
```

## 自定义配置

### 修改个人信息
在 Supabase 中更新 `profiles` 表，或修改 `src/pages/Home.js` 中的默认数据。

### 添加新项目
在 Supabase 中插入 `projects` 表，或修改 `src/pages/Projects.js` 中的示例数据。

### 自定义样式
编辑 `tailwind.config.js` 修改主题配置：
```javascript
theme: {
  extend: {
    colors: {
      primary: '#3B82F6',    // 主色调
      secondary: '#10B981',  // 次要色调
      accent: '#F59E0B',     // 强调色
    },
  },
}
```

## 开发指南

### 添加新页面
1. 在 `src/pages/` 创建新组件
2. 在 `src/App.js` 中添加路由
3. 在 `src/components/Header.js` 中添加导航链接

### 数据库操作
使用 `src/services/database.js` 中的服务：
- `profileService` - 个人信息操作
- `projectService` - 项目信息操作
- `contactService` - 联系信息操作

## 性能优化

- 使用 React.lazy 进行代码分割
- 图片优化和懒加载
- Supabase 查询优化
- Netlify 缓存配置

## 安全考虑

- 环境变量管理敏感信息
- Supabase RLS（行级安全）配置
- 表单验证和 XSS 防护
- CORS 配置

## 故障排除

### 常见问题
1. **Supabase 连接失败**: 检查环境变量配置
2. **数据加载失败**: 确认数据库表已创建
3. **样式不显示**: 检查 Tailwind CSS 配置

### 调试技巧
- 使用浏览器开发者工具
- 检查网络请求
- 查看 Supabase 日志
- Netlify 部署日志

## 许可证

MIT License

## 联系方式

如有问题或建议，请通过以下方式联系：
- 邮箱: contact@example.com
- GitHub: [您的GitHub用户名]
- 项目地址: [项目仓库地址]

---

**截止时间**: 2025年11月23日17:00
**技术要求**: Supabase + Netlify + 至少3页面 + 至少3数据表